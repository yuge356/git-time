# 2026-08-07 工作日志

## 时间统计页 3 项调整（AnalyticsView.vue）

1. **"完成今日任务"卡片改为今日口径**：数据源从 range 汇总（completed_task_count/total_task_count）改为当日 check-in（`dailyPlanService.checkIn(today)` 的 completed_items/total_items），即"今日计划清单中添加的项目/任务"数量；完成率徽标同步改。
2. **任务分布环形图改为今日数据**：新增 `todaySummary`（`analyticsService.summary(today, today)`），环形图与中心总投入改用当日数据；标题改"今日时间投入占比 / 今日投入"；跨天自动刷新（`startDayRolloverWatch`，30s 轮询日期变化；预设窗口随之滑动，自定义区间不动）。
3. **柱状图默认最近 10 天 + 默认日粒度**：dateFrom 默认 today-9，预设首项改为"10 天"（去掉 7 天），`selectedGranularity` 默认 'day'，进入页面先看到"每日投入时间"。

验证：vue-tsc 通过、vite build 通过（3.17s）、dev 服务器 5174 已启动且代理健康检查 ok。

## 项目演进备忘（8/1 后有大量更新）
- 前端固定端口 5174（strictPort），vite.config 代理 /api 与 /health 到 8000，并有 vite.local-backend 插件管理后端生命周期。
- scripts/start-local.cmd（.ps1）一键启动：先 alembic upgrade head，再起后端 8000 + 前端 5174，含健康检查与看门狗（-Watch）。
- AnalyticsView 已重写为 metric-card/圆环/weekly-chart 新结构；auth store 有 showPageIntros；timer store 有 completedRevision；Task 模型新增 node_type（PROJECT/TASK）。
- 8/1 后端改动（check-in learning_seconds 统计当日全部 session，按用户时区）：pytest 46 全过。

## 个人资料页通栏 + 统计页"计划项可见性"（上午追加）

4. **个人资料页通栏**：`.profile-page` 从双栏（左标题右卡片）改为单列通栏，用户信息卡片占满页面宽度（main.css）。
5. **今日计划项始终出现在时间统计页**（backend/app/services/analytics.py）：`build_analytics_summary` 的 task_distribution 在 session 分布基础上，并入所选日期范围内"已加入当日清单但未计时"的计划项（seconds=0，按 task_id/title 去重）；从清单删除（软删）即不再出现；已计时条目不受影响。
6. **任务删除仍留项目历史**：核查确认无需改动——任务删除是软删除，session 保留，`resolve_project`/`task_titles` 基于含已删任务的 all_tasks，项目历史与分布中标题可正常解析。
7. E2E 全场景验证通过（未计时出现/计时后更新/删清单项消失/删项目后历史保留 600s），pytest 63 全过，测试脚本用后已删。后端已重启生效。
8. 层级规则备忘：任务树固定 PROJECT → MODULE → TASK 三级（API 直接建 TASK 必须挂 MODULE 下）。
