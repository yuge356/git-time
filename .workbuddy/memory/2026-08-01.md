# 2026-08-01 工作日志

## 服务无法访问 + 用户需求 1/3 复核

1. **ERR_CONNECTION_REFUSED**：昨天的前后端后台进程已结束，服务不会自启。已重启：后端 uvicorn 8000、前端 vite 5173（strictPort）。
2. **需求 1（页面合并）与需求 3（计时同步）复核验证**：
   - 后端 API 端到端实测（注册→建任务→建计划→加计划项→RUNNING→COMPLETED 600s）：任务 actual_seconds=600 ✓、计划项 actual_seconds=600 ✓（脚本用后已删）。
   - dev 服务器实际服务内容核验：AppShell 无"学习计时"导航、含 app-sidebar 新布局；路由 / → /today、/timer → /today；TodayView 含计时模块。全部为新代码。
   - 结论：功能本身正常，用户看到旧界面大概率是浏览器标签页缓存了改动前的旧版本，需 Ctrl+F5 强刷。
3. **强化需求 3**：TodayView 新增 `liveExtra`/`displayActual`——计时运行中，当日清单该项的"已学习"和"剩余用时"每秒实时跳动（基于 displaySeconds 与最后持久化快照的差值，每次计时动作后由服务器数据重新校准）。
4. 验证：vue-tsc 通过、vite build 通过（2.64s）。

## 备忘
- 本地预览需手动启动两个进程：后端 `./.venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000`（backend/ 下），前端 `./node_modules/.bin/vite --port 5173`（frontend/ 下）。会话结束进程即终止，再次预览需重启。
