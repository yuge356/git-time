# 2026-09-01

## 重新设计登录前欢迎页（WelcomeView）

- 重写 `frontend/src/views/WelcomeView.vue` + `styles/main.css` 中的「公开欢迎页」样式块（原 1063–1724 行整体替换）。
- 布局：hero 由 `0.68fr / 1.55fr` 两列改为视觉主导，图片（产品截图 + 任务树 demo）约占 70% 宽度，文字约 30%，符合「图片占比大于文字」的要求。
- 结构：吸顶导航（滚动后毛玻璃）→ hero → 三张核心能力卡 → 渐变 CTA 条 → 页脚。
- 动效（均为低强度、可降级）：
  - 背景三团紫/粉/蓝极光光斑缓慢漂移（26–32s）
  - hero 文案按 `--rise` 依次淡入上移（`welcome-rise` / `welcome-fade`）
  - 产品预览卡指针跟随倾斜（外层 transform transition，±5°/+4°）
  - 内层 `.welcome-stage` 上下缓浮 8s；轮播帧交叉淡入 + 轻微缩放
  - 两张悬浮数据卡错开延迟浮动；能力卡滚动进入时依次 reveal
  - 全套动效在 `prefers-reduced-motion: reduce` 下关闭
- 关键坑：
  1. `.welcome-page { overflow: hidden }` 会让 `position: sticky` 的导航失效（创建了滚动容器）→ 改为 `overflow-x: clip`，背景极光层改 `position: fixed`。
  2. tilt 的 transform（transition）与入场/浮动 animation 会互相覆盖 → 分层：外层 tilt、内层 float。
  3. 文件后段（原 2243–2387 行）残留旧 `.welcome-features` / `.welcome-slide` 响应式规则，已清理，仅保留 onboarding 部分与 brand__copy；共享玻璃拟态块中的 `.welcome-showcase` / `.welcome-features` 也已摘除，避免覆盖新样式。
  4. `highlights` 常量与 DOM ref 同名导致 TS2451，ref 更名 `highlightsSection`。
- 验证：`vue-tsc --build --force` 通过；`vite build` 通过（438ms）；dev server 5174 已提供新代码。
